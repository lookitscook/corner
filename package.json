{
  "name": "corner-gradient-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^22.12.0 || >=24.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:standalone": "npm run build && node scripts/build-standalone.js",
    "preview": "vite preview",
    "test": "npm run test:unit && npm run test:browser",
    "test:unit": "node --test corner-gradient/tests/engine.test.js",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "dat.gui": "^0.7.9"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^8.3.0"
  }
}
